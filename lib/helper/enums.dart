enum OrderStatusType {Accepted, Delivered, Pending}
enum recordDialogStatus {Edit, Add, View}
enum searchBy {Id, MobileNumber, Code, Name}
enum roles {User, Admin, CompanyUser}
enum refillStatus {All, Success, Fail}